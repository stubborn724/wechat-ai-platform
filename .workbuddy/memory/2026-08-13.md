# 2026-08-13

## 将本地项目接入为 WorkBuddy Skill
- 用 skill-creator 流程把 `wechat-ai-platform` 平台注册为项目级 skill：
  `.workbuddy/skills/wechat-ai-content-operations/`
- 结构：`SKILL.md`（操作规则 + 完整工作流）、`references/api_reference.md`
  （全部 19 个 API 模块端点目录）、`scripts/check_health.py`（健康检查+登录验证）。
- 打包产物：`tmp/wechat-ai-content-operations.zip`（校验通过）。
- 关键事实修正：本地 API 实际端口是 **8002**（`backend/app/config.py`），
  README 写的 8000 已过时；前端 5173。平台恢复脚本 `scripts/start-local-platform.ps1`。
- 现状：平台当前未运行（check_health 连接 8002 被拒绝）。

## 用 skill 发布第一篇公众号文章（含关键 bug 修复）
- 流程：拉起平台 → admin 登录 → 确认账号「我的家具号(165)」→ 联网选题
  （2026年8月第二周 AI 产业大事件）→ `POST /articles/create`（publish_mode=draft）
  → 管线 7 分钟生成 → **草稿已保存到微信草稿箱**（status=draft_saved, id=255）。
- **修复重大 bug**：`POST /articles/create` 稳定 500、文章卡 pending。根因：
  后端代码 `print` 带 emoji（📝✅❌），Windows 中文系统 stdout 重定向到文件后
  用 GBK 编码抛 `UnicodeEncodeError` → FastAPI 500。
  修复：`backend/app/main.py` 顶部强制 stdout/stderr `reconfigure(utf-8, errors=replace)`；
  `scripts/start-local-platform.ps1` 启动 API 前设 `PYTHONIOENCODING=utf-8`、`PYTHONUTF8=1`。
- 部署事实：文本生成走 kuai 中转（gpt-5-mini），图片走 kuai_openai_compatible，无需 DashScope key；
  WeChat 通道为 relay 模式（草稿/发布可用，草稿箱同步/评论/私信暂不可用）。
- 已清理：3 条 500 残留 pending 文章（252/253/254）；临时 token/请求文件已删。
- skill 已补充该环境坑与部署说明（Failure Handling + Local Deployment Notes 章节）。

- 收尾核实：文章 255 最终状态 draft_saved/DRAFT_SAVED，无报错，封面图已生成。
  注意：详情接口是 GET /articles/{task_id}（用 task_id 而非文章 id，用错会 404）。
